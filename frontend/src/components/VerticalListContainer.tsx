import React from "react";
import styled from "styled-components";

const VerticalListContainerDiv = styled.div`
  
`;

const VerticalListContainer = () => {
  return (
    <VerticalListContainerDiv>
      
    </VerticalListContainerDiv>
  )
}

export default VerticalListContainer;