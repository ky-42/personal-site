import React from 'react';
import styled from 'styled-components';

const SocialsListSection = styled.section`
    
`;

const SocialListHeader = styled.h2`
    
`;

const SocialsList = styled.div`
    
`;

const SocialsPage = () => {
    return (
        <SocialsListSection>
            <SocialListHeader>
                Socials
            </SocialListHeader>
            <SocialsList>
                {/* TODO create social link component */}
            </SocialsList>
        </SocialsListSection>
    );
};

export default SocialsPage;